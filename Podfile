# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RSBeautyCamera' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
  pod 'Masonry', '~> 0.6.2'
  pod 'SDWebImage', '~>3.7'
  pod 'GPUImage', '~> 0.1.7'
  # Pods for RSBeautyCamera

  target 'RSBeautyCameraTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RSBeautyCameraUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
